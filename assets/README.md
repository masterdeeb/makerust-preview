# Assets Directory

Place all your fonts, images, 3D models, and audio files here. Bevy will automatically load from this folder when using `asset_server.load("...")`.

Example:
```rust
let font = asset_server.load("fonts/Inter-Regular.ttf");
```